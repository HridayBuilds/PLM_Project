package com.odoo.plm.enums;

public enum ProductStatus {
    DRAFT,      // Initial creation, not yet finalized
    ACTIVE,     // Current active version
    ARCHIVED    // Old version, replaced by newer version
}
